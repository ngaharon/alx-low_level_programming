#include <stdio.h>
/**
 * main - Entry point
 *
 */

int main(void)
{
	char b;

	int c;

	b = 'a';
	while
		(b < 10) {
			putchar(c + '0');
			c++;
		}
	while
		(b <= 'f') {
			putchar(b);
			b++;
		}
	putchar('\n');
	return (0);
